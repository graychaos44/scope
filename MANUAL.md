# SCOPE 매뉴얼

명령별 사용법과 **출력을 읽는 법**입니다. 수치를 어떻게 해석해야 하는지가 이 문서의 핵심입니다.

---

## 공통 설정

환경변수로 조건을 바꿉니다. 조건은 모든 기록에 그대로 남습니다.

| 변수 | 기본값 | 뜻 |
|---|---|---|
| `EP` | 100 | 시드마다 몇 판 |
| `SEEDS` | `20000,30000,40000` | 시드 목록 |
| `MAXSTEP` | 400 | 한 판 최대 수 |
| `CAL_DAYS` | 14 | 검측기 교정 유효기간(일) |

```bash
EP=1000 SEEDS=20000,30000,40000 python scope.py 검사 my_game
```

★**판정에 쓸 값은 1000판 이상 × 시드 3개**를 권합니다. 적게 재면 방향이 거꾸로 나옵니다 —
실제로 30판에서 −1%, 1000판에서 −9.7% 가 나온 적이 있습니다.

---

## `검사` — 게임이 과제로 성립하는가

```bash
python scope.py 검사 my_game
```

무작위로 굴리면서 **매 수를 기록**하고, 그 기록만 읽어 여섯 가지를 봅니다.

| 판정 | 뜻 | 무엇을 고쳐야 하나 |
|---|---|---|
| `NOWON` | 끝난 판이 승패를 안 알려준다 | `info["won"]` 을 넣는다. **이게 없으면 승률이 조용히 0이 된다** |
| `NOWIN?` | 승패 키는 있는데 한 판도 못 이겼다 | 어려운 것인지 못 재는 것인지 갈리지 않는다. 승리 조건이 실제로 만들어지는지 확인 |
| `DEADCELL` | 관측 칸이 처음부터 끝까지 안 변한다 | 그 칸은 정보가 0이다. 채우거나 `obs_reserved` 로 선언 |
| `RANGE` | 관측 값이 상한(기본 10)을 크게 넘는다 | 정규화가 깨진 것. 학습이 그 칸에 끌려간다 |
| `EPLEN` | 한 판이 평균 5수 미만 | 첫 수부터 끝낼 수 있다는 뜻. 최소 진행 구간을 강제 |
| `NOTERM` | 최대 수를 다 쓰도록 안 끝난다 | 시간초과 종료를 넣는다 |

**출력 읽기**

```
FAIL  판정 2건 | 무작위승률   0.0% 평균    2.0수 미종료 0 | 관측 5칸 버튼 5
      [NOWIN?] ...
      [EPLEN] 한 판이 평균 2.0수 ...
```

- `무작위승률` — 0%면 배울 신호가 없고, 너무 높으면 과제가 아닙니다
- `평균 N수` — 판 길이. 짧으면 과제 붕괴를 의심
- `미종료` — 안 끝난 판 수

---

## `기준선` — 그냥 쉬운 게임은 아닌가

```bash
python scope.py 기준선 my_game
```

**이 명령을 빠뜨리면 안 됩니다.** 어댑터가 90%를 냈다고 좋아했는데 버튼 하나만 눌러도 93%였던
사고가 실제로 있었습니다.

```
  무작위           15.0%
  상수 버튼0        0.0%
  상수 버튼4       93.3%     ← 이것
  화면가림+무작위   2.0%
★이 게임의 기준선 = 무작위 15.0% · 상수 최고 93.3%(버튼4)
```

| 값 | 읽는 법 |
|---|---|
| 무작위 | 아무렇게나 눌렀을 때 |
| **상수 최고** | ★**한 버튼만 계속 눌렀을 때의 최고값.** 어떤 정책이든 이걸 넘어야 한다 |
| 화면가림+무작위 | 화면을 안 보고도 이겨지는 정도 |

**어떤 정책이 "배웠다"고 말하려면 무작위와 상수 최고를 둘 다 넘어야 합니다.**

---

## `능력` — 무엇을 보고 푸는가

```bash
python scope.py 능력 my_game path/to/checkpoint.pt
```

게임을 **한 가지씩 건드려서** 얼마나 못하게 되는지 봅니다.

| 건드림 | 많이 떨어지면 |
|---|---|
| 화면가림 | 화면을 본다 (지각) |
| 버튼씹힘 | 타이밍이 중요하다 |
| 한수지연 | 반응 속도로 푼다 |
| 화면잡음 | 정확한 값이 필요하다 |
| 칸섞기 | 그 칸을 실제로 본다 |

```
그대로            96.7%
화면가림            1.7%  하락  +95.0%p     ← 화면을 본다
버튼씹힘           95.0%  하락   +1.7%p     ← 타이밍은 안 중요
칸별 중요도 상위: 칸1=+81.7 · 칸0=+79.2     ← 이 두 칸으로 푼다

기준선: 무작위 3.3% · 상수최고 0.0% → 어댑터가 +93.3%p
```

★★**두 가지를 같이 봐야 합니다.** 하락폭이 커도 **기준선을 못 넘으면 능력의 증거가 아닙니다.**
안 보고도 이기는 게임에 정책을 대면 화면 가릴 때 크게 떨어지지만, 그 게임은 애초에 지각이
필요 없습니다. SCOPE는 기준선을 못 넘으면 경고를 먼저 띄웁니다.

---

## `해부` — 안에서 무엇이 바뀌었나

```bash
python scope.py 해부 core.pt              # 지금 상태
python scope.py 해부 core.pt 이전판.pt     # 이전과 비교
```

**읽기만 합니다.** 모델을 고치지 않습니다.

| 나오는 것 | 뜻 |
|---|---|
| 죽은유닛비율 | 출력이 항상 0인 유닛 |
| 층norm 중앙값·최대·최소 | 값 폭주나 수축 |
| 정규화키수 | 정규화 통계가 들어 있나 |
| **바뀐키 / 사라진키** | ★학습·증류가 **어디를 바꿨나**. 사라진 키가 있으면 경보 |

★**사라진 키 경보가 중요합니다.** 증류가 가치 헤드를 매번 지우고 있었는데, 성능이 −92%p 날 때까지
아무도 몰랐던 적이 있습니다. 이전 판과 비교하면 그 자리에서 잡힙니다.

★내부 지표는 **표시만** 합니다. 성능과의 상관이 확인되기 전에는 합격·불합격을 매기지 않습니다.
지표가 좋아져도 성능이 안 오르고, 성능이 올라도 지표가 안 움직이는 경우를 실제로 겪었습니다.

---

## `작업대` — 목록·배치·교정

```bash
python scope.py 작업대 목록                    # 무엇이 밀렸나 + 교정 상태
python scope.py 작업대 등록 my_game 게임 "이유"  # 검사 목록에 올린다
python scope.py 작업대 실행                    # 목록에 있는 것을 한 번에
python scope.py 작업대 교정                    # ★검측기 자신을 정답지로 대조
```

★**등록만 하고 안 돌리면 소용없습니다.** 실제로 게임을 목록에 등록해 놓고 한 번도 안 돌려서,
상수로 이겨지는 게임을 못 잡고 판정이 뒤집힌 적이 있습니다.

**교정**은 결함을 일부러 심어둔 시험 게임으로 검측기 자신을 시험합니다.
잡아야 할 것을 잡고 정상에서 오탐이 없으면 그날 날짜를 찍습니다.

```
  OK  정상 게임        기대 [] → 나옴 []
  OK  죽은 칸 게임      기대 [DEADCELL] → 나옴 [DEADCELL]
교정 성공 — 검사들에 오늘 날짜를 찍었다
```

**교정 기한(기본 14일)이 지난 검사의 결과는 판정에 쓰지 않습니다.**

---

## `비교` — 두 실행을 같은 자로

```bash
python scope.py 비교 <run_id> <run_id>
```

★**조건이 다르면 거부합니다.**

```
★비교 거부 — 조건이 다르다 — 비교가 성립하지 않는다
   episodes: 60  vs  30
```

같은 사안에 판정이 세 번 뒤집힌 적이 있는데, 원인은 데이터도 모델도 아니라 **매번 조건이 달랐던 것**
이었습니다. 그래서 도구가 먼저 막습니다.

---

## `현황` · `기록` · `보고`

```bash
python scope.py 현황                 # 최근 실행 목록
python scope.py 기록 <run_id>        # 그 실행의 조건·지표·산출물
python scope.py 보고 <run_id>... --형식 md
```

모든 실행은 `runs/<run_id>/run.json` 과 실행 대장(`SCOPE_LEDGER.tsv`)에 남습니다.
대장은 나중에 "설정 → 결과" 예측 모델의 재료가 됩니다.

---

## 기록 형식

실행 하나 = **설정(params) + 지표(metrics) + 산출물(artifacts) + 꼬리표(tags)**.

```python
from scopelib.record import Run

with Run("내검사", params={"env": "my_game", "episodes": 1000}) as r:
    r.metric("result.winrate", 88.0, unit="percent_0_100")
    r.artifact("checkpoint.pt")
```

**규칙 세 가지**

1. **단위는 필수** — 처음 보는 지표에 단위를 안 주면 거부합니다. 승률을 한쪽은 0~1, 한쪽은
   0~100으로 재서 검사 26건이 허위로 울린 적이 있습니다.
2. **출처를 적습니다** — `measured`(실제로 굴림) / `simulated`(시뮬 모델) / `predicted`(예측 모델).
   섞이면 판정이 통째로 오염됩니다.
3. **항목 수에 상한이 없습니다** — 카테고리·지표는 필요하면 늘립니다. 대신 **등록제**로 난립을 막습니다.

---

## 자주 겪는 문제

| 증상 | 원인 | 처방 |
|---|---|---|
| 승률이 항상 0% | `info["won"]` 이 없다 | `검사` 가 `NOWON` 으로 잡아줍니다 |
| 검사가 아무것도 안 잡는다 | 검측기가 고장났을 수 있다 | `작업대 교정` 을 돌려 확인 |
| 값이 잴 때마다 다르다 | 조건이 다르다 | `비교` 가 조건 차이를 지목합니다 |
| 성능이 좋아 보이는데 이상하다 | 그냥 쉬운 게임일 수 있다 | `기준선` 으로 상수 승률 확인 |
| 판정이 `판정불가` | 신뢰구간이 겹친다 | 표본(EP)을 늘립니다. **실패가 아닙니다** |
